return {
	client = {
		keys = require("bindings.client.keys"),
		-- mouse = require("bindings.client.mouse"),
	},
	global = {
		keys = require("bindings.global.keys"),
		mouse = require("bindings.global.mouse"),
	},
	mods = require("bindings.mods"),
}
